# Ansible Playbook for Celestia Light Node

## Requirements
 - Installed Ansible on local machine
 - Remote machine OS Should be Ubuntu

## How to use?
 - Fill out the `hosts` file
 - Run playbook
```
ansible-playbook -i hosts playbook.yml
```
